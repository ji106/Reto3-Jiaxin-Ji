<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pelicula.titulo %></title>
</head>
<body>
    <div class="pelicula">
        <h1><%= pelicula.title %> (<%= pelicula.year %>)</h1>
        <p>Género: <%= pelicula.genre %></p>
        <p>Director: <%= pelicula.director %></p>
        <p>Descripción: <%= pelicula.description %></p>

        <h3>Copias disponibles:</h3>
        <ul>
            <% pelicula.copies.forEach(c => { %>
                <li>
                    ID copia: <%= c.copyId %> |
                    Formato: <%= c.format %> |
                    Estado: <%= c.status %>
                </li>
            <% }) %>
        </ul>
    </div> 

    <br><a href="/peliculas">Volver al listado</a>
</body>
</html>